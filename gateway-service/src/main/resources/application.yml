spring:
  application:
    name: gateway-service
  datasource:
    url: jdbc:h2:mem:gatewaydb
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
account:
  service:
    url: http://localhost:8081
